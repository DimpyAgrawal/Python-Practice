a={}
size=int(input("enter size of dictionary"))
for i in range(0,size):
    x=input("enter key")
    y=input("enter value")
    a[x]=y
temp=[]
res=dict()
for key,val in a.items():    
    if val not in temp:
        temp.append(val)
        res[key]=val
print(res)
